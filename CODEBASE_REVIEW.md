# Paper2Slides 코드베이스 리뷰 (2026-02-28)

## 1) Executive Summary
- 본 코드베이스는 `CLI + FastAPI + React` 3계층으로 구성되어 있으며, 핵심 로직은 `rag → summary → plan → generate` 파이프라인으로 명확히 분리되어 있습니다.
- 현재 설계는 **단일 세션 직렬 처리 안정성**에 초점을 맞추고 있고, 체크포인트 재사용/재개 UX가 잘 구현되어 있습니다.
- 다만 운영 관점에서 **오류 경계 처리, 상태 조회 비용, 입력 포맷 확장성(비-PDF), 로깅 일관성**은 우선 개선이 필요합니다.

## 2) 아키텍처 진단

### 2.1 Pipeline 계층
- `run_pipeline`은 stage 상태(`pending/running/completed/failed/cancelled`)를 파일에 저장하며, 실패 시 다음 단계로 진행하지 않는 안전한 fail-stop 방식입니다.
- `detect_start_stage`를 통해 체크포인트 존재 기반으로 재개 지점을 결정합니다.
- 장점: 재시도 비용 절감, 재생성 범위 제어 가능.
- 리스크: stage별 예외 유형이 일괄 문자열로 저장되어 장애 분류가 어려움.

### 2.2 API 계층
- `SessionManager`로 동시 실행을 1건으로 제한하여 리소스 충돌 위험을 줄입니다.
- `/api/chat`는 즉시 응답 후 background task로 파이프라인 실행하여 프론트 폴링 모델과 자연스럽게 결합됩니다.
- `/api/status/{session_id}`는 상태 파일을 탐색해 진행률을 조회합니다.
- 리스크: status 조회 시 `rglob("state.json")` 전체 탐색이 커질수록 느려질 수 있음.

### 2.3 Frontend 계층
- `ChatWindow` 단일 컨테이너가 대화, 파일, 설정, 폴링, 취소 등 다수 책임을 보유합니다.
- 장점: 기능 통합과 빠른 개발.
- 리스크: 파일 길이/복잡도 증가로 유지보수 및 회귀 테스트 비용 상승.

## 3) 강점 (What is working well)
1. **체크포인트 기반 재개 전략**: 중단 후 이어서 실행하는 사용자 경험이 우수합니다.
2. **세션 취소 경로 확보**: API 취소 요청과 stage 시작 전 취소 검사로 안전장치가 있습니다.
3. **CLI/API 설정 모델 유사성**: config 키 체계가 비슷해 운영 일관성이 좋습니다.

## 4) 핵심 리스크 (Top Risks)

### R1. 입력 포맷과 실제 처리 경로 불일치
- README는 다양한 문서 포맷 지원을 홍보하지만, API 파이프라인 진입부는 PDF 존재를 강제합니다.
- 영향: 사용자 기대와 실제 동작 불일치, 지원 이슈 증가.

### R2. 상태 조회의 스케일 리스크
- session 상태 확인 시 후보 디렉토리 전역 탐색이 발생합니다.
- 영향: 프로젝트 수 증가 시 폴링 지연/서버 부하.

### R3. 로깅 경로 혼합
- `logger`와 `print`가 혼재되어 운영 로그 수집/분석 일관성이 저하됩니다.
- 영향: 장애 분석 시간 증가.

### R4. 오류 모델 단순화
- `error` 문자열 1개 중심 저장으로 사용자 친화 메시지와 내부 디버그 정보를 분리하기 어렵습니다.
- 영향: 프론트 오류 UX 및 관찰 가능성(Observability) 제한.

### R5. 프론트 단일 컴포넌트 비대화
- `ChatWindow`가 지나치게 많은 책임을 가짐.
- 영향: 변경 영향 범위 확대, 테스트 난이도 상승.

## 5) 우선순위 개선 로드맵

### P0 (즉시)
1. 상태 파일 인덱싱 도입 (`session_id -> state_path`)으로 `/api/status` 조회 O(1)에 근접화.
2. API/파이프라인 로깅을 `logger` 중심으로 일원화.
3. README 지원 포맷 문구와 실제 처리 경로를 정합화(또는 비-PDF 처리 경로 구현).

### P1 (단기)
1. 오류 스키마 구조화 (`code`, `user_message`, `debug_message`, `stage`).
2. `ChatWindow` 기능 분리: polling hook, session hook, file preview hook로 책임 분산.
3. 상태 전이 테스트(정상/실패/취소) 추가.

### P2 (중기)
1. 멀티 세션 큐잉(예: FIFO) 옵션 제공.
2. 상태 저장소 추상화(파일 → Redis/DB 전환 가능 계층).
3. 메트릭 수집(평균 stage 시간, 실패율, 취소율).

## 6) Governance 권고
- 운영 표준 문서에 다음을 추가 권장:
  - 상태 파일 스키마 버전(`state_schema_version`)
  - 로그 Correlation ID (`session_id` 전 구간 전파)
  - README claim과 runtime capability 간 정합성 검증 체크리스트

## 7) 결론
- 전체적으로 MVP를 넘어 **실사용 가능한 자동화 파이프라인**에 도달해 있습니다.
- 다음 단계는 기능 추가보다 **운영 신뢰성/관측성/구조적 유지보수성** 강화가 ROI가 더 큽니다.
